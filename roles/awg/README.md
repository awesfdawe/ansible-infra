Role for setting up Amnezia WireGuard.

**Role Variables:**

- `awg_interface` (default: `awg0`) - The desired network interface name

- `awg_template` - Path to the configuration template

- `awg_port` (default: `none`) - The port or service to open for server installations
