- name: Install Certbot and Cloudflare DNS plugin packages
  ansible.builtin.apt:
    pkg:
      - certbot
      - python3-certbot-dns-cloudflare
    update_cache: true
    cache_valid_time: 3600
  become: true

- name: Ensure Let's Encrypt configuration directory exists
  ansible.builtin.file:
    path: /etc/letsencrypt
    state: directory
    owner: root
    group: root
    mode: "0755"
  become: true

- name: Deploy Cloudflare DNS API provider configuration
  ansible.builtin.template:
    src: templates/cloudflare.ini.j2
    dest: /etc/letsencrypt/cloudflare.ini
    owner: root
    group: root
    mode: "0600"
  become: true

- name: Acquire SSL wildcard certificates via Cloudflare DNS challenge
  ansible.builtin.command:
    cmd: >
      certbot certonly
      --dns-cloudflare
      --dns-cloudflare-credentials /etc/letsencrypt/cloudflare.ini
      --cert-name {{ domain }}
      -d {{ domain }}
      -d *.{{ domain }}
      --deploy-hook "systemctl reload nginx"
      --agree-tos
      --email {{ email }}
      --non-interactive
    creates: "/etc/letsencrypt/live/{{ domain }}/fullchain.pem"
  become: true
