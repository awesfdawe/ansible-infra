---
- name: Copy ntfy Config
  ansible.builtin.template:
    src: ntfy-server.yml.j2
    dest: /etc/ntfy/server.yml
    owner: root
    group: root
    mode: "0644"
  become: true

- name: Create needed folders for ntfy
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: _ntfy
    group: _ntfy
    mode: "0755"
  loop:
    - /var/cache/ntfy/attachments
    - /var/lib/ntfy
  become: true

- name: Enable ntfy
  ansible.builtin.systemd_service:
    name: ntfy
    enabled: true
    state: started
  become: true

- name: Add ntfy config to nginx
  ansible.builtin.template:
    src: nginx/sites/ntfy.conf.j2
    dest: /etc/nginx/conf.d/ntfy.conf
    owner: root
    group: root
    mode: "0644"
  notify: Reload Nginx
  become: true
