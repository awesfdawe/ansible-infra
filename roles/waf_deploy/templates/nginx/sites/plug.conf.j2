# Managed by Ansible
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name {{ domain }}; 

    include snippets/proxy.conf;

    location / {
        root /var/www/html;
        index index.html;
    }
}